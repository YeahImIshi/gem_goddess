<h1>Explore the newest collection!</h1>

<h2>Featured Products</h2>
<% @products.each do |product| %>
  <div>
    <%= link_to product.title, product %>
    <p><%= product.description %></p>
    <p><%= number_to_currency(product.price) %></p>
  </div>
<% end %>